# Allow all crawlers
User-agent: *
Allow: /

# Disallow admin or private paths (if any in future)
# Disallow: /admin
# Disallow: /api/private

# Sitemap location
Sitemap: https://mettglobal.com/sitemap.xml

# Crawl-delay (optional, helps prevent server overload)
Crawl-delay: 1
